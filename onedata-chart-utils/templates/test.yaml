# apiVersion: v1
# kind: Service
# metadata:
#    labels:
#     app: {{ template "fullname" . }}
#     chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
#     release: "{{ .Release.Name }}"
#     heritage: "{{ .Release.Service }}"
#     app: {{ template "service_url" . }}